# 部署历史说明

这份文档仅作历史参考。

当前正式方案是：

- 托管平台：GitHub Pages
- 编辑入口：站内自研 `/admin/`
- 内容格式：Markdown + LaTeX
- 发布方式：GitHub Actions 自动构建和发布

如果以后要重新迁移到 Netlify，可以把这份文档当作参考，但当前仓库不再依赖 Netlify Identity 或 Decap CMS。
